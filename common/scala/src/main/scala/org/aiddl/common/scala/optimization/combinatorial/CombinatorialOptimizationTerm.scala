package org.aiddl.common.scala.optimization.combinatorial

import org.aiddl.core.scala.representation.Sym

object CombinatorialOptimizationTerm {
  val Cost = Sym("cost")
  val Min = Sym("min")
  val Max = Sym("max")
}
