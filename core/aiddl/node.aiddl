(#mod self org.aiddl.node)

;; Module requests
;; (GET_ENTRIES ?SourceNode ?Module)
;; -> (ENTRIES ?Module [...])
;; (PUT_ENTRY ?SourceNode ?Module ?Entry)
;; SUCCESS, (FAILURE ?Code)
;; (DEL_ENTRY ?SourceNode ?Module ?Entry)
;; SUCCESS, (FAILURE ?Code)

;; (PROVIDES (?MODULES ?FUNCTIONS))

;; (#def Message
;;   (