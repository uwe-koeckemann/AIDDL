package org.aiddl.core.scala.tools

import scala.collection.mutable
/*
object Profiler {
  private val counters = new mutable.HashMap[String, Long]

  def probe( depth: Int ) = {
    val stElems = Thread.currentThread().getStackTrace
    val origin = new StringBuilder()
    origin.append( stElems(2).getClass.getSimpleName )
    origin.append(".")


  }

  private def shortestClassName( )
}
*/